# Frontend

Next.js 14 dashboard for Forecast Money. See `AGENTS.md` for development context.

```bash
npm install
npm run dev   # http://localhost:3000
```

API base: `http://localhost:8000/api/v1` (override with `NEXT_PUBLIC_API_URL`).
